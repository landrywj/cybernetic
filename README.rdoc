ReadME

git clone git@github.com:landrywj/cybernetic
cd cybernetic
bundle install
cp database.example.yml to database.yml
cp secrets.example.yml to secrets.yml
rake secret (add to secrets.yml)
update database.yml for your database server
rake db:setup
